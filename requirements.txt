aiogram>=3.13,<4
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
pydantic>=2.9
pydantic-settings>=2.4

# PostgreSQL on alwaysdata. Commented out so a local SQLite setup does not need
# to build it; uncomment before deploying (see DEPLOY-alwaysdata.md).
# asyncpg>=0.29
