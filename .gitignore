# Secrets — the bot token lives here and must never be committed.
.env

.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Student data — never commit real answers or results.
data/
*.json.tmp

.DS_Store
